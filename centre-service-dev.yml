spring:
  datasource:
    username: medicine
    password: root
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/medicine_db
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

server:
  port: 8031
  servlet:
    context-path: /centre-service